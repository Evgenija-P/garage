import Image from 'next/image';

export default function Home() {
  return (
    <main className="w-full flex min-h-screen flex-col items-center justify-between p-24">
      <button className="w-16 h-10 bg-accent">button `:)` </button>
    </main>
  );
}
